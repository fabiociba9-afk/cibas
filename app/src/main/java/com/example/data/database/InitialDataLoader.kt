package com.example.data.database

/**
 * InitialDataLoader is kept empty so the application starts with a completely empty database.
 * The system starts from scratch and prompts for the creation of the first Super Administrator.
 */
object InitialDataLoader {
    // No mock or pre-filled records. Database starts 100% empty.
}
